export enum CommandName {
  ADD = 'add',
  AUDIT = 'audit',
  COMPLETE = 'complete',
  FIX = 'fix',
  LIST = 'list',
  NEXT = 'next',
  RENDER = 'render',
  SHOW = 'show',
  SUPERSEDE = 'supersede',
  VALIDATE = 'validate',
}
